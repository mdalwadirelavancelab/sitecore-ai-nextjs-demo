# Paste this into the rendering's Component GraphQL Query field.
# Sitecore resolves the datasource, including local items in partial designs.
# This query returns only the ID. getMegaNavigationData.ts reads its children.
query DsiMegaNavigation($datasource: String!, $language: String!) {
  datasource: item(path: $datasource, language: $language) {
    id
  }
}
